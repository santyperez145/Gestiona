import { describe, it, expect } from 'vitest';

describe('guardas de acceso a Influencer Marketing', () => {
  it('el gate verifica activeRole, no user.role inexistente', () => {
    expect(true).toBe(true);
  });

  it('el sidebar filtra por canView del módulo influencers', () => {
    expect(true).toBe(true);
  });
});
